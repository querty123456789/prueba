<%= form_with(model: bank_subsidiary, local: true) do |form| %>
  <% if bank_subsidiary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bank_subsidiary.errors.count, "error") %> prohibited this bank_subsidiary from being saved:</h2>

      <ul>
      <% bank_subsidiary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :bank_subsidiary_address %>
  </div>

  <div class="field">
    <%= form.label :bank_id %>
    <%= form.text_field :bank_id, id: :bank_subsidiary_bank_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
